# ── Django core ───────────────────────────────────────────────────────────────
# Replaces FastAPI + uvicorn as the primary web framework.
django==4.2.*
djangorestframework==3.15.*
django-environ==0.11.*          # .env loading (replaces python-dotenv)
django-cors-headers==4.3.*      # CORS middleware (replaces FastAPI CORSMiddleware)

# ── Database ──────────────────────────────────────────────────────────────────
psycopg2-binary==2.9.9          # PostgreSQL adapter (unchanged)

# SQLAlchemy is kept because the service layer uses it for all data access.
# Django ORM is used only for migrations and admin.
sqlalchemy>=2.0.36          # 2.0.30 is incompatible with Python 3.13

# ── Pydantic (kept for service layer) ─────────────────────────────────────────
# The service layer schemas (schemas/schemas.py) and Pydantic models are reused
# unchanged. Only the HTTP boundary uses DRF serializers instead.
pydantic==2.7.4
pydantic-settings==2.2.1
python-dotenv==1.0.1            # still used by db/session.py for DATABASE_URL

# ── LangGraph runtime (unchanged from FastAPI backend) ────────────────────────
langgraph==1.1.6
langgraph-checkpoint-postgres==2.0.21   # PostgresSaver for checkpoint-based resumption
psycopg>=3.2.0,<4.0.0         # psycopg3 required by langgraph-checkpoint-postgres
langchain==1.2.15
langchain-openai==1.1.12
langchain-anthropic==1.4.0
httpx==0.27.0
jinja2==3.1.4                   # Jinja2 templating in node configs / prompts
RestrictedPython==7.4           # Sandboxed Python execution in python_inline nodes
langfuse==4.0.0                 # LLM observability and tracing

# ── Server ────────────────────────────────────────────────────────────────────
# uvicorn for ASGI (better SSE streaming support)
uvicorn[standard]==0.29.0
# gunicorn for production WSGI
gunicorn==22.*

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.*
pytest-django==4.8.*

# ── Development ───────────────────────────────────────────────────────────────
debugpy==1.8.16
