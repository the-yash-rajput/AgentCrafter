{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Attach Backend (Docker debugpy)",
      "type": "debugpy",
      "request": "attach",
      "connect": {
        "host": "localhost",
        "port": 5678
      },
      "pathMappings": [
        {
          "localRoot": "${workspaceFolder}/backend_django",
          "remoteRoot": "/app"
        }
      ],
      "justMyCode": false
    }
  ]
}
